import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nyp.myApps',
  appName: 'myApps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
